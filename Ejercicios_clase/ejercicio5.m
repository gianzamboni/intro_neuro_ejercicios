function [vOrdenado] = ejercicio5(a,b)
    if a < b
        vOrdenado = [a,b]
    else
        vOrdenado = [b,a]
    end
        
%    switch a < b
%        case 0:
%            vOrdenado = [b,a]
%        case 1: 
%            vOrdenado = [a,b]
%    end
end